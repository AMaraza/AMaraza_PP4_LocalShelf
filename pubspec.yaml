name: local_shelf
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  cloud_firestore: ^6.7.0
  firebase_core: ^4.12.0
  flutter:
    sdk: flutter
  geolocator: ^14.0.2
  http: ^1.6.0
  location: ^8.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
